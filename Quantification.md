# AI 量化交易系统（增强版需求文档 v2.7）

> **当前状态**：已完成基础行情爬虫（Python + GitHub Pages 雏形）、`quant_system` 重构、自选股分析与盘中实时（方案 A+B）  
> **目标**：构建「数据 → 因子 → 回测 → 实盘 → AI Agent → 可视化」的完整闭环量化系统  
> **本次迭代**：实现单股 Decision Engine MVP，将预测、因子、回测、Agent、数据质量和持仓上下文合成为指导性操作建议

---

## 目录

- [一、系统总体架构](#一系统总体架构)
  - [1.3 MVP 闭环定义](#13-mvp-闭环定义)
  - [1.4 目标工程结构](#14-目标工程结构)
  - [1.5 阶段能力矩阵](#15-阶段能力矩阵)
- [二、阶段拆分](#二阶段拆分)
  - [Phase 0：数据基建（P0）](#phase-0数据基建p0)
  - [Phase 1：多源数据扩展（P1）](#phase-1多源数据扩展p1)
  - [Phase 2：因子系统（P1–P2）](#phase-2因子系统p1p2)
  - [Phase 3：回测系统（P2）](#phase-3回测系统p2)
  - [Phase 4：实盘交易系统（P2）](#phase-4实盘交易系统p2)
  - [Phase 5：AI 量化 Agent](#phase-5ai-量化-agent)
  - [Phase 6：Web 可视化系统（P2）](#phase-6web-可视化系统p2)
- [三、股票走势预测能力路径](#三股票走势预测能力路径)
- [四、优秀量化系统优化蓝图](#四优秀量化系统优化蓝图)
- [五、当前实现检查与代码优化记录](#五当前实现检查与代码优化记录)
- [六、软硬件与运行环境需求](#六软硬件与运行环境需求)
- [七、Agent 接入方案](#七agent-接入方案)
- [八、通用约束与风险声明](#八通用约束与风险声明)
- [九、版本迭代记录](#九版本迭代记录)

---

## 一、系统总体架构

### 1.1 核心数据流

```
爬虫层（行情 / 舆情 / 研报 / 产业链）
        ↓
数据清洗治理层（P0）
        ↓
统一数据仓库（MySQL / TSDB / Redis）
        ↓
因子计算引擎（技术 / 情绪 / 机构 / 产业）
        ↓
策略层（选股 / 择时 / 风控）
        ↓
回测引擎
        ↓
实盘交易系统
        ↓
AI Agent 分析层
        ↓
Web 可视化平台
```

### 1.2 与现有工程映射（`quant_system/`）

| 层级 | 当前模块 | 状态 |
|------|----------|------|
| 爬虫层 | `data_source/`（东财 / 新浪降级、分钟线） | 部分完成 |
| 清洗治理 | `pipeline/cleaner`, `validator`, `normalizer`, `quality_inspector` | 基础完成 |
| 交易日历 | `utils/trade_calendar.py` | 已完成 |
| 质量门禁 | `pipeline/quality_gate.py`, `tasks/inspect_job.py`, `pipeline/cross_source.py` | ✅ 已完成（含跨源 diff） |
| 存储 | `storage/json_store`（Redis / MySQL 预留） | JSON 已用，MySQL/Redis 可选 |
| 盘中分析 | `pipeline/intraday_analyzer`, `tasks/intraday_job`, `reports/live/` | ✅ 已完成 |
| 日级分析 | `pipeline/stock_analyzer`, `tasks/stock_job` | ✅ 已完成 |
| 技术因子 | `factors/technical.py`, `tasks/factor_job.py` | ✅ MVP 已完成 |
| 多因子合成 | `factors/composite.py`, `factors/fundamental.py` | ✅ 技术+情绪+基本面+资金 |
| 舆情 | `data_source/sentiment_api.py`, `tasks/sentiment_job.py` | ✅ 东财评论+雪球热榜 |
| 数据增强 | `data_source/enhance_api.py`, `tasks/enhance_job.py` | ✅ 估值/公司行为/资金/指数 |
| 回测引擎 | `backtest/`, `strategy/`, `tasks/backtest_job.py` | ✅ 含 P2-4 容量/归因/滚动验证 |
| 走势预测 | `prediction/verified.py`, `tasks/predict_job.py` | ✅ L2（5d 可验证预测） |
| 操作决策 | `decision/`, `tasks/decision_job.py`, `reports/decision/` | ✅ 指导性操作建议 MVP（买入/持有/减仓/卖出/观望） |
| Agent | `agent/`, `tasks/agent_job.py`, `reports/agent/` | ✅ 规则型解释（P4-1） |
| 风控 | `risk/`（仓位计算） | 🔶 基础规则已接入回测，无账户级组合风控 |
| 模拟交易 | `trading/`, `tasks/sim_trade_job.py`, `reports/trading/` | ✅ P3-1 MVP 已完成（虚拟账户/订单/持仓/复盘） |
| 调度 | `scheduler/cron_runner` | ✅ 已完成 |

### 1.3 MVP 闭环定义

> **MVP 目标**：优先打通「日线数据 → 清洗 / 复权 → 技术因子 → 单策略回测 → 报告输出」闭环，先验证数据可信度和策略评估能力，再扩展舆情、研报、实盘和 Agent。

**MVP 范围**

- 日线 K 线数据清洗、去重、缺失检查、前复权。
- A 股交易日历统一。
- 技术因子库 MVP：MA、MACD、RSI、ATR、动量、均线突破。
- 单策略回测 MVP：T+1、手续费、印花税、滑点、涨跌停和停牌处理。
- 输出回测报告：收益曲线、最大回撤、夏普比率、胜率、盈亏比、交易明细。

**MVP 暂不包含**

- 自动实盘下单。
- Tick 级高频回测。
- 多 Agent 自动决策闭环。
- 复杂组合优化和机器学习模型训练。

**MVP 验收标准**

- 可对不少于 100 只 A 股运行最近 3 年日线回测。
- 回测结果可重复，固定输入数据与参数时输出一致。
- 每次回测可追溯使用的数据版本、因子版本、策略版本和参数。
- 对低质量数据可主动告警或拒绝进入因子 / 回测流程。

### 1.4 目标工程结构

```text
quant_system/
  data_source/      # 数据采集：行情、舆情、研报、产业链
  pipeline/         # 清洗、标准化、复权、质量校验
  storage/          # JSON / MySQL / TSDB / Redis 存储适配
  factors/          # 因子计算与因子版本管理
  strategy/         # 选股、择时、调仓、组合规则
  backtest/         # 回测引擎、成交撮合、绩效分析
  prediction/       # 可验证走势预测、概率输出、预测复盘
  decision/         # 单股指导性操作建议、仓位建议、失效条件
  risk/             # 回测与实盘共用风控规则
  portfolio/        # 持仓、现金、净值、敞口、再平衡
  trading/          # 模拟交易、实盘接口、订单、成交、账户同步
  agent/            # 数据巡检、策略分析、选股解释、舆情解读
  web/              # 行情、因子、舆情、回测、实盘可视化
  tasks/            # 定时任务与批处理入口
  monitoring/       # 任务监控、告警、审计、权限、配置管理
```

**模块边界**

- `data_source` 只负责采集原始数据，不直接做策略判断。
- `pipeline` 负责清洗、标准化、复权和质量评分。
- `factors` 只消费标准化后的可信数据，不直接调用爬虫。
- `strategy` 只消费行情、因子和基础面数据，不处理原始数据清洗。
- `backtest` 不依赖实盘交易接口，避免回测与实盘逻辑耦合。
- `risk` 同时服务回测和实盘，保证两端风控规则一致。
- `prediction` 只能基于已通过回测验证的策略或因子组合输出概率化预测。
- `decision` 汇总预测、因子、回测、Agent、质量和持仓上下文，只输出指导性建议，不直接下单。
- `trading` 只执行已通过风控的订单，不能绕过 `risk`。
- `monitoring` 负责采集、回测、预测、交易和 Agent 行为的运行状态与审计记录。

### 1.5 阶段能力矩阵

| 阶段 | 核心目标 | 支持功能 | 可回答的问题 | 预测能力 |
|------|----------|----------|--------------|----------|
| Phase 0 数据基建 | 数据可用、可信、可计算 | 日线 / 分钟线采集、清洗、复权、交易日历、质量评分、补爬 | 这只股票的数据是否完整可信？今天是否能参与分析？ | 不做走势预测，只提供可信输入 |
| Phase 1 多源数据 | 扩展信息维度 | 舆情、研报、产业链、财报、指数、龙虎榜等数据接入 | 这只股票是否有事件、情绪或机构预期变化？ | 可形成事件和情绪观察，不输出交易级预测 |
| Phase 2 因子系统 | 把数据转成可量化信号 | 技术、情绪、机构、产业因子计算与排名 | 这只股票当前动量、趋势、情绪、估值是否占优？ | 具备初级方向判断能力，但尚未验证胜率 |
| Phase 3 回测系统 | 验证信号有效性 | 单策略 / 多因子回测、成交模拟、绩效分析、风险过滤 | 某类信号过去是否有效？胜率、回撤、收益风险比如何？ | 具备可验证的走势预测能力，可输出概率化方向判断 |
| Phase 4 实盘交易 | 小规模实盘验证 | 实时行情、交易接口、风控、持仓、订单日志 | 回测有效的信号在真实成交中是否仍有效？ | 具备实盘监控下的短周期预测和执行能力 |
| Phase 5 AI Agent | 自动分析和解释 | 数据巡检、策略诊断、选股解释、舆情解读、参数建议 | 为什么看多 / 看空？哪些证据支持？策略为什么失效？ | 具备解释型预测能力，默认不自动下单 |
| Phase 6 Web 可视化 | 统一操作入口 | 看板、因子排名、预测结果、回测报告、实盘监控 | 哪些股票值得关注？预测、证据、风险和历史表现如何？ | 提供可读、可追踪、可对比的预测结果展示 |

**关键结论**

- Phase 0 只解决“数据能不能信”，不应该输出走势判断。
- Phase 2 可以输出“信号强弱”和“趋势倾向”，但不能直接视为有效预测。
- Phase 3 完成后，系统才具备最低可接受的股票走势预测能力，因为预测信号经过历史回测验证。
- Phase 4 完成后，系统才具备实盘可用的预测闭环，但仍需人工授权和风控拦截。
- Phase 5 让预测具备解释能力，输出“方向 + 概率 + 证据 + 风险”，而不是只给涨跌结论。

---

## 二、阶段拆分

### Phase 0：数据基建（P0）

> **目标**：让所有数据「可用、可信、可计算」——必须优先完成。

#### 2.1 数据清洗与标准化

**功能模块**

- K 线异常数据过滤（涨跌停异常、停牌、0 成交量）
- 去重（分钟级 / 日级）
- 缺失数据补齐
- 股票代码统一（6 位标准）
- 交易日历统一（A 股）

**标准 K 线结构**

```json
{
  "ts": "",
  "code": "",
  "open": 0,
  "high": 0,
  "low": 0,
  "close": 0,
  "volume": 0,
  "amount": 0,
  "adj_factor": 1
}
```

**字段约束**

- `ts`：ISO 8601 时间字符串，默认使用 Asia/Shanghai 时区。
- `code`：6 位 A 股股票代码，不包含交易所后缀。
- `open/high/low/close`：单位为人民币元，保留原始精度。
- `volume`：成交量，单位为股。
- `amount`：成交额，单位为人民币元。
- `adj_factor`：复权因子，未复权时默认 `1`。

#### 2.2 复权系统

- 前复权
- 后复权
- 分红送股修复
- `adj_factor` 版本管理
- 复权公式、数据来源和更新时间必须可追溯

#### 2.3 数据校验与质量监控

- 跨源一致性校验（东财 vs 新浪）
- 延迟 / 缺失告警
- 数据版本与采集时间戳
- 低质量数据隔离，禁止直接进入因子与回测

**数据质量评分结构**

```json
{
  "code": "000001",
  "trade_date": "2026-07-01",
  "missing_rate": 0,
  "duplicate_count": 0,
  "cross_source_diff": 0.001,
  "quality_score": 98,
  "status": "ok",
  "checked_at": ""
}
```

**质量规则**

- `quality_score >= 90`：可进入因子与回测。
- `70 <= quality_score < 90`：可用于展示，但回测需显式允许。
- `quality_score < 70`：隔离并触发告警 / 补爬。
- 跨源价格差异超过阈值时，以数据更新时间、成交量完整性和来源优先级综合判定。

#### 2.4 统一数据仓库

- MySQL：元数据、财报、研报
- TSDB：分钟 / Tick 时序
- Redis：盘中实时缓存（`live:stock:{code}`）

#### 2.5 数据源契约

| 数据 | 优先来源 | 频率 | 目标存储 | 降级策略 |
|------|----------|------|----------|----------|
| 日 K | 东财 | 每日收盘后 | JSON / TSDB | 新浪兜底，失败则告警 |
| 分钟线 | 东财 | 盘中定时 | Redis / JSON | 缺失标记，不参与盘中信号 |
| 指数数据 | 东财 | 每日 / 盘中 | JSON / TSDB | 新浪兜底 |
| 财报 | 待定 | 季度 | MySQL | 人工补录 / 延迟更新 |
| 舆情 | 雪球 | 定时采集 | MySQL / JSON | 跳过并记录失败 |
| 研报 | 待定 | 每日 | MySQL | 延迟更新 |

**通用采集要求**

- 所有原始数据必须保留 `source`、`fetched_at`、`raw_version`。
- 清洗后的数据必须保留 `data_version`。
- 多源降级必须在日志和质量报告中记录，不能静默覆盖。

#### 2.6 基础数据补齐

- PE / PB / ROE
- 财报数据
- 龙虎榜
- 解禁 / 分红 / 业绩预告
- 指数数据（沪深 300 / 中证 500）
- 利率数据

---

### Phase 1：多源数据扩展（P1）

#### 3.1 舆情系统（雪球为核心）

**数据结构**

```json
{
  "code": "",
  "title": "",
  "content": "",
  "likes": 0,
  "comments": 0,
  "author": "",
  "time": "",
  "platform": ""
}
```

**情绪分类**：看多 / 看空 / 中性

**输出指标**

- 多空比
- 热度指数
- 情绪加速度

#### 3.2 机构研报系统

**采集字段**

- 目标价
- 评级变化
- 分析师信息
- 覆盖机构数

**输出因子**

- 目标价溢价率
- 一致预期价
- 评级动量

#### 3.3 产业链题材系统

**重点赛道**

- AI
- 半导体
- 电子特气
- 稀缺金属

**输出**

- 赛道 → 事件 → 个股映射

---

### Phase 2：因子系统（P1–P2）

#### 4.1 技术因子

- MA / MACD / RSI / KDJ
- ATR / 波动率
- 动量因子
- 均线突破

#### 4.2 情绪因子

- 多空情绪比
- 热度变化率
- 情绪背离

#### 4.3 机构因子

- 目标价空间
- 评级变化
- 机构覆盖度

#### 4.4 产业因子

- 涨价强度
- 缺货指数
- 事件驱动评分

#### 4.5 因子标准输出

```json
{
  "code": "",
  "trade_date": "",
  "factors": {
    "ma20_bias": 0,
    "rsi14": 0,
    "sentiment_score": 0,
    "target_price_premium": 0
  },
  "updated_at": ""
}
```

#### 4.6 因子计算规范

- 因子输入只能来自清洗、复权、质量校验后的标准数据。
- 因子计算不得使用未来数据，所有窗口只能包含当前交易日及之前的数据。
- 每个因子必须有名称、公式说明、参数、版本、适用频率和缺失值处理规则。
- 公式或参数默认值变更时，必须生成新的因子版本。
- 上市不足 N 天、停牌、成交额过低等场景需要明确输出 `null`、跳过或降权。

**因子明细输出结构**

```json
{
  "code": "000001",
  "trade_date": "2026-07-01",
  "factor_name": "rsi14",
  "factor_value": 62.5,
  "factor_version": "1.0.0",
  "data_version": "daily_kline_20260701",
  "updated_at": ""
}
```

#### 4.7 初级走势信号输出

Phase 2 完成后，可以输出技术层面的初级走势信号，但该信号只代表“因子倾向”，不能直接等同于可交易预测。

```json
{
  "code": "000001",
  "trade_date": "2026-07-01",
  "horizon": "5d",
  "signal": "bullish",
  "signal_score": 72,
  "drivers": ["ma20_breakout", "rsi14_neutral_up", "volume_expansion"],
  "limitations": ["not_backtested", "technical_only"],
  "updated_at": ""
}
```

**信号解释**

- `signal`：`bullish`、`bearish`、`neutral` 三类方向。
- `signal_score`：0-100 分，只表示当前因子强度。
- `horizon`：预测观察周期，如 `1d`、`5d`、`20d`。
- `limitations`：必须标明是否经过回测、是否只包含技术因子。

---

### Phase 3：回测系统（P2）

#### 5.1 回测引擎能力

- T+1 交易规则
- 手续费 / 滑点
- 涨跌停限制
- 停牌处理
- 回测与实盘共用风控模块

#### 5.2 策略类型

- 技术策略
- 情绪策略
- 机构策略
- 产业链策略
- 多因子策略

#### 5.3 绩效指标

- 年化收益
- 最大回撤
- 夏普比率
- 胜率
- 盈亏比

#### 5.4 回测成交规则

**默认成交假设**

- 买入价：信号日后第一个可交易日开盘价。
- 卖出价：触发卖出信号后第一个可交易日开盘价。
- T+1：当日买入持仓不可当日卖出。
- 涨停：买入日涨停且无法成交时，订单取消或顺延，需由策略参数指定。
- 跌停：卖出日跌停且无法成交时，继续持有并记录未成交原因。
- 停牌：停牌期间不可买卖，价格沿用最近可交易日用于净值估算。

**交易成本默认值**

- 佣金：万分之三，最低 5 元。
- 印花税：卖出单边千分之一。
- 滑点：默认 5bp，可按策略覆盖。

#### 5.5 组合与风控规则

- 单票最大仓位：默认 10%。
- 单行业 / 单题材最大暴露：默认 30%。
- 最低流动性门槛：近 20 日日均成交额不低于指定阈值。
- 排除 ST、退市整理、新股上市不足 N 日标的。
- 最大回撤熔断：触发后停止开新仓或降仓。
- 黑名单机制：支持手动和规则生成。

#### 5.6 可验证走势预测输出

Phase 3 完成后，系统开始具备“可验证”的走势预测能力。这里的预测不是保证涨跌，而是基于历史统计和当前因子状态输出概率、置信度、收益风险比和失效条件。

```json
{
  "code": "000001",
  "trade_date": "2026-07-01",
  "horizon": "5d",
  "direction": "up",
  "probability": 0.61,
  "confidence": "medium",
  "expected_return": 0.028,
  "max_expected_drawdown": 0.045,
  "strategy_version": "trend_breakout_v1.0.0",
  "factor_versions": {
    "ma20_bias": "1.0.0",
    "rsi14": "1.0.0"
  },
  "evidence": {
    "backtest_win_rate": 0.57,
    "backtest_profit_loss_ratio": 1.35,
    "sample_count": 420
  },
  "risk_flags": ["earnings_nearby", "sector_high_volatility"],
  "updated_at": ""
}
```

**最低验收标准**

- 至少支持 `1d`、`5d`、`20d` 三类观察周期之一，MVP 优先 `5d`。
- 每个预测必须关联策略版本、因子版本和数据版本。
- 预测必须有历史样本数量，样本过少时只能输出 `low_confidence`。
- 必须同时输出方向、概率、置信度、预期收益、预期回撤和风险标记。
- 不允许输出“必涨”“必跌”等确定性结论。

---

### Phase 4：实盘交易系统（P2）

#### 6.1 行情系统

- WebSocket 实时行情
- Tick 数据订阅

#### 6.2 交易系统

- 下单 / 撤单
- 持仓查询
- 批量交易

#### 6.3 风控系统（核心）

- 单票仓位限制
- 最大回撤熔断
- 高频交易限制
- 黑名单机制
- 实盘交易前必须经过 `risk/` 统一风控校验
- 所有下单、撤单、拒单、风控拦截必须记录可回放日志

---

### Phase 5：AI 量化 Agent

#### 7.1 数据巡检 Agent

- 自动检测缺失数据
- 自动触发补爬
- 输出数据质量报告和补爬记录

#### 7.2 策略分析 Agent

- 策略失效分析
- 参数优化建议
- 参数建议必须附带回测证据，不直接改写生产参数

#### 7.3 选股 Agent

**输入示例**：「AI 产业链 + 机构上调 + 舆情爆发」

**输出**

- 股票池
- 逻辑解释
- 使用的数据、因子、事件和历史表现证据

#### 7.4 舆情解读 Agent

- 市场情绪日报
- 热点聚类
- 分歧分析

#### 7.5 Agent 权限边界

- 默认只允许分析和建议，不允许自动下单。
- 若开启交易权限，必须经过人工确认、风控校验和审计日志。
- Agent 输出必须可解释，至少包含数据来源、判断依据、时间范围和置信度。
- 禁止仅凭自然语言结论直接触发实盘交易。
- 所有 Agent 行为需要记录输入、输出、引用数据和执行结果，支持事后回放。

---

### Phase 6：Web 可视化系统（P2）

**模块**

- 行情看板
- 因子排名
- 舆情热度
- 机构研报
- 赛道监控
- 实盘监控
- 回测分析

---

## 三、股票走势预测能力路径

### 3.1 预测能力分级

| 等级 | 达成阶段 | 能力描述 | 输出形式 | 是否可用于交易决策 |
|------|----------|----------|----------|--------------------|
| L0 | Phase 0 | 数据质量判断 | 数据完整性、质量分、异常标记 | 否 |
| L1 | Phase 2 | 因子倾向判断 | 看多 / 看空 / 中性信号、因子分数 | 否，只能作为观察 |
| L2 | Phase 3 | 回测验证预测 | 方向、概率、置信度、收益风险比、历史胜率 | 可用于模拟交易和人工辅助 |
| L3 | Phase 4 | 实盘验证预测 | 实时信号、订单可执行性、风控结果 | 可用于小资金实盘，但需授权 |
| L4 | Phase 5 | 解释型预测 | 预测结论、证据链、风险、失效条件、策略诊断 | 可用于投研决策，不默认自动交易 |
| L5 | Phase 6 | 产品化预测 | 看板、排名、预警、回测对比、实盘跟踪 | 可作为统一操作入口 |

### 3.2 预测闭环定义

股票走势预测必须形成以下闭环：

```text
可信数据
  → 因子信号
  → 策略规则
  → 历史回测
  → 当前预测
  → 风险过滤
  → 结果展示 / 模拟交易 / 实盘验证
  → 预测复盘
```

若缺少“历史回测”和“预测复盘”，只能称为信号提示，不能称为完整预测能力。

### 3.3 预测输出边界

**允许输出**

- 未来 `1d / 5d / 20d` 的方向倾向。
- 上涨 / 下跌概率。
- 置信度：`low`、`medium`、`high`。
- 预期收益区间和预期回撤。
- 关键驱动因子和风险因素。
- 策略历史表现和样本数量。

**禁止输出**

- 确定性涨跌结论。
- 没有数据版本、策略版本和回测证据的预测。
- 绕过风控的买卖建议。
- 由 Agent 直接触发的默认实盘下单。

### 3.4 到哪一步能完成走势预测

- **Phase 2 完成后**：可以完成“初级走势信号”，例如趋势偏多、偏空、震荡，但还不能判断该信号历史上是否赚钱。
- **Phase 3 完成后**：可以完成“可验证走势预测”，这是系统首次达到可接受的预测能力门槛。预测结果应包含方向、概率、置信度、历史胜率、收益风险比和风险标记。
- **Phase 4 完成后**：可以完成“可执行走势预测”，即预测结果能接入实时行情、交易约束和风控，但仍需要人工授权。
- **Phase 5 完成后**：可以完成“可解释走势预测”，即系统不仅输出预测，还能说明为什么、依据什么、什么情况下预测失效。
- **Phase 6 完成后**：可以完成“产品化走势预测”，在 Web 看板中展示股票池、预测排名、证据链、回测表现和实盘跟踪。

**近期最小目标**：完成 Phase 3 的单策略回测 MVP 后，即可开始对股票输出 `5d` 维度的可验证走势预测。

### 3.5 指导性操作建议

在实时性低优先级、指导性高优先级的场景下，系统不应只输出“涨跌预测”，而应进一步生成单股操作建议：

```text
预测结果
  → 因子状态
  → 回测证据
  → Agent 解释
  → 数据质量
  → 模拟持仓
  → Decision Engine
  → 买入 / 持有 / 减仓 / 卖出 / 观望
```

**操作建议输出**

```json
{
  "code": "600378",
  "action": "watch|buy|hold|reduce|sell",
  "position_suggestion": 0.12,
  "confidence": "medium",
  "reasons": [],
  "risks": [],
  "invalid_conditions": [],
  "requires_human_review": true
}
```

**边界**

- 操作建议只用于研究、复盘和模拟交易。
- `buy / reduce / sell` 必须要求人工复核。
- 决策层不得绕过风控或直接调用真实交易接口。
- 数据质量不足、缺少预测或回测证据时，应输出 `watch` 或低置信度建议。

---

## 四、优秀量化系统优化蓝图

> **目标**：不推翻当前阶段计划，而是在已落地的“数据 → 因子 → 回测 → 预测”闭环上，逐步补齐优秀量化系统应有的研究、交易、风控、解释、监控和产品化能力。

### 4.1 核心能力清单

| 能力 | 优化目标 | 当前承接模块 | 后续落点 |
|------|----------|--------------|----------|
| 数据可信 | 数据完整、字段统一、可追溯、可校验 | `pipeline/`, `storage/` | 跨源 diff、数据血缘、异常补爬 |
| 因子可复用 | 因子有版本、参数、公式、输入输出规范 | `factors/` | 因子注册表、因子分布监控、因子漂移检测 |
| 策略可复现 | 固定数据、参数、版本后结果一致 | `strategy/`, `backtest/` | 策略配置化、参数快照、实验记录 |
| 回测可信 | 交易成本、T+1、涨跌停、停牌、流动性约束完整 | `backtest/`, `risk/` | 动态股票池、成交量约束、退市 / ST 处理 |
| 预测可验证 | 预测关联样本、胜率、收益风险比和复盘结果 | `prediction/` | 滚动验证、命中率统计、失效原因归因 |
| 风控前置 | 回测、模拟、实盘共用风控规则 | `risk/` | 账户级风控、组合敞口、行业集中度 |
| 执行可审计 | 下单、撤单、拒单、风控拦截均可回放 | `trading/` | 模拟交易、实盘网关、订单审计 |
| 组合可管理 | 支持多股票、多策略、多账户组合视角 | `portfolio/` | 现金、持仓、净值、再平衡、收益归因 |
| 结果可解释 | 输出信号来源、因子贡献、风险因素、失效条件 | `agent/`, `web/` | 选股解释、策略诊断、预测复盘报告 |
| 监控可告警 | 数据、任务、预测、交易、风控异常可告警 | `monitoring/` | 任务状态、数据延迟、订单异常、回撤告警 |

### 4.2 数据系统优化

**数据范围扩展**

- 行情：日 K、分钟线、Tick、盘口、成交明细。
- 指数：宽基指数、行业指数、主题指数。
- 基本面：财报、估值、盈利能力、成长性、资产负债。
- 公司行为：分红、送转、配股、停复牌、解禁、退市、ST。
- 资金：成交额、换手率、主力资金、北向资金、融资融券。
- 舆情：新闻、公告、雪球 / 股吧讨论、热度、情绪。
- 机构：研报、评级、目标价、覆盖机构、盈利预测。
- 产业链：题材、事件、上下游关系、价格变化。
- 宏观：利率、汇率、商品价格、流动性指标。

**优化要求**

- 区分原始层、清洗层、特征层、结果层。
- 采集失败必须支持重试、告警、降级。
- 多源数据不可静默覆盖，必须保留来源、差异和最终采用规则。
- 所有进入因子、回测、预测的数据必须通过质量门禁。

### 4.3 因子与策略优化

**因子扩展方向**

- 技术因子：趋势、动量、波动率、突破、量价背离。
- 基本面因子：估值、盈利、成长、现金流、负债。
- 情绪因子：多空比、热度、评论增速、情绪分歧。
- 机构因子：目标价空间、评级变化、覆盖机构数、盈利预测变化。
- 产业因子：事件强度、涨价强度、供需紧张度、题材热度。
- 风险因子：波动率、回撤、流动性、集中度、尾部风险。

**策略扩展方向**

- 技术趋势策略。
- 均值回归策略。
- 多因子选股策略。
- 事件驱动策略。
- 情绪反转 / 情绪动量策略。
- 行业轮动策略。
- 大盘择时策略。
- 组合再平衡策略。

**策略输出要求**

- 候选股票池。
- 买入 / 卖出 / 观望信号。
- 仓位建议。
- 触发原因。
- 风险标记。
- 策略版本和参数快照。

### 4.4 回测与预测优化

**回测增强**

- 动态股票池。
- 流动性约束。
- 成交量容量约束。
- 退市、ST、新股过滤。
- 多策略对比。
- 收益归因。
- 参数敏感性分析。
- 滚动回测和样本外验证。

**预测增强**

- 支持 `1d / 5d / 20d` 多周期预测。
- 输出方向、概率、置信度、预期收益、预期回撤。
- 输出关键驱动因子和风险因素。
- 记录策略历史表现和样本数量。
- 支持预测复盘，统计命中率、收益表现和失效原因。

**最小可用预测定义**

- 数据通过质量门禁。
- 因子无未来函数。
- 策略经过回测。
- 回测包含交易成本、T+1、停牌、涨跌停。
- 预测输出方向、概率、置信度、样本数量和风险标记。
- 预测结果支持事后复盘。

### 4.5 风控、组合与交易优化

**风控规则**

- 单票最大仓位。
- 行业 / 题材集中度限制。
- 最大回撤熔断。
- 单日亏损限制。
- 流动性过滤。
- 波动率过滤。
- ST / 退市 / 黑名单过滤。
- 重大事件窗口降仓。
- 高频交易频率限制。
- 账户级风险敞口控制。

**组合管理**

- 当前持仓、目标持仓、调仓差异。
- 现金、净值、回撤、收益曲线。
- 行业暴露、风格暴露、策略贡献。
- 组合再平衡和收益归因。

**交易系统**

- 先建设模拟交易：虚拟账户、模拟订单、模拟成交、模拟持仓。
- 再建设实盘辅助：券商接口、订单状态、成交回报、账户同步。
- 默认不允许自动下单；开启自动交易必须显式授权。
- 所有订单必须经过风控，风控拒单不可绕过。

### 4.6 AI Agent 与 Web 产品化优化

**Agent 类型**

- 数据巡检 Agent：发现缺失、异常、延迟，并触发补爬。
- 因子分析 Agent：检查因子分布、漂移、失效。
- 策略诊断 Agent：分析收益来源、回撤原因、参数敏感性。
- 选股解释 Agent：解释为什么某只股票进入股票池。
- 舆情解读 Agent：总结市场热点、情绪分歧和事件影响。
- 复盘 Agent：对预测、交易和风控结果做事后复盘。

**Web 页面**

- 市场总览。
- 自选股看板。
- 个股详情页。
- 因子排名。
- 策略信号。
- 走势预测。
- 回测报告。
- 模拟交易。
- 实盘监控。
- 风控面板。
- 数据质量面板。
- Agent 分析报告。

### 4.7 监控、审计与运维优化

**监控指标**

- 数据采集成功率。
- 数据延迟。
- 任务运行耗时。
- 回测 / 预测任务失败率。
- 实盘订单失败率。
- 风控拦截次数。
- 系统资源使用情况。

**告警场景**

- 数据源失效。
- 行情延迟。
- 质量评分过低。
- 任务连续失败。
- 回撤超限。
- 订单异常。
- 账户资金异常。

**审计要求**

- 数据版本审计。
- 策略参数审计。
- 回测结果审计。
- 预测输出审计。
- 订单和成交审计。
- Agent 操作审计。

### 4.8 优化后的阶段交付

| 阶段 | 名称 | 核心交付 | 能力边界 | 状态 |
|------|------|----------|----------|------|
| 已完成 | MVP 闭环 | 数据巡检、技术因子、回测、5d 预测、看板 | 可研究、可展示、可复盘 | ✅ |
| Next 1 | 数据增强 | 跨源 diff、公司行为、指数、资金、财报基础数据 | 提升数据可信度和覆盖面 | ✅ |
| Next 2 | 因子增强 | 基本面、资金、情绪因子合成 | 从技术单因子扩展到多维信号 | ✅ |
| Next 3 | 策略增强 | 多因子策略、策略对比 | 提升策略丰富度 | 🔶 多因子策略已有，行业轮动/事件驱动未做 |
| Next 4 | 回测增强 | 动态股票池、容量约束、收益归因、滚动验证 | 提升回测可信度 | ✅ |
| Next 4.5 | 单股决策 | 操作建议、仓位建议、理由、风险、失效条件 | 把预测转为指导性操作建议 | ✅ Decision MVP |
| Next 5 | 模拟交易 | 虚拟账户、模拟订单、模拟持仓、交易复盘 | 验证实时运行和执行偏差 | ✅ P3-1 MVP |
| Next 6 | 实盘辅助 | 券商接口、交易前风控、订单审计 | 可辅助实盘，不默认自动交易 | ⏳ |
| Next 7 | Agent 与平台化 | 策略诊断、选股解释、预测复盘、统一看板 | 提升解释、复盘和日常使用效率 | ✅ 规则型 Agent |

---

## 五、当前实现检查与代码优化记录

### 5.1 当前功能实现检查

| 需求方向 | 当前实现 | 结论 | 后续动作 |
|----------|----------|------|----------|
| 数据采集 | 东财 / 新浪降级、分钟线、watchlist 自动补齐 | 已满足 MVP | 继续补公司行为、财报、资金、指数覆盖面 |
| 数据质量 | `quality_inspector`、`quality_gate`、跨源 diff、自动 backfill | 已满足 P0 | 增加数据血缘和长期质量趋势 |
| 技术因子 | MA、MACD、RSI、ATR、动量、初级信号 | 已满足 P1-1 | 增加因子注册表和因子漂移监控 |
| 多因子 | 技术、情绪、基本面、资金合成 | 已具备基础能力 | 增加行业中性化、因子 IC / 分层测试 |
| 回测 | MA 金叉、多因子策略、容量约束、归因、滚动验证 | 已满足 P2-4 基础目标 | 增加动态股票池和组合级回测 |
| 预测 | `5d` 方向、概率、置信度、回测证据 | 已满足 L2 MVP | 增加 `1d / 20d`、预测复盘统计 |
| 操作决策 | 汇总预测、因子、回测、Agent、质量和持仓上下文 | 已满足指导性建议 MVP | 下一步补建议复盘和参数化策略 |
| Agent | 规则型选股解释、策略诊断、预测复盘 | 已满足 P4-1 基础目标 | 接入外部 LLM Agent 时保持证据链和权限边界 |
| 风控 | 仓位计算和基础规则已接入回测 | 部分满足 | 补账户级风控、组合敞口、熔断规则 |
| 模拟交易 | `trading/` 已实现虚拟账户、订单、持仓和报表 | 已满足 P3-1 MVP | 下一步补组合级复盘和风险指标 |
| Web 报表 | 行情、个股、因子、预测、增强、Agent 报表 | 已满足静态看板 | 增加统一交互式控制台 |

### 5.2 代码优化记录

本轮检查发现 Agent 选股解释对输入数据类型依赖较强：当 JSON 中分数或估值字段以字符串、空值或异常文本出现时，可能导致 Agent 单股报告失败。

**已优化**

- `agent/stock_explainer.py` 增加安全数值转换。
- `multi_factor_score`、`signal_score`、`pe_ttm` 支持数字字符串和异常值兜底。
- 异常估值字段不再中断 Agent 报告生成。
- 新增单元测试覆盖字符串分数和不可解析估值场景。

**验证结果**

- `quant_system/tests/test_agent.py`：通过。
- `quant_system/tests` 全量测试：通过。

---

## 六、软硬件与运行环境需求

### 6.1 本地开发环境

| 项目 | 最低要求 | 推荐要求 | 说明 |
|------|----------|----------|------|
| 操作系统 | macOS / Linux | macOS Apple Silicon 或 Linux x86_64 | 当前 README 以 macOS + Homebrew Python 为主 |
| Python | 3.12 | 3.12.x | 使用项目 `.venv`，避免系统 Python 污染 |
| CPU | 2 核 | 4 核以上 | 日线采集、因子、回测可满足 |
| 内存 | 8 GB | 16 GB 以上 | 多股票回测和报表生成更稳定 |
| 磁盘 | 5 GB | 20 GB 以上 SSD | JSON 数据、历史 K 线、报表、测试缓存 |
| 网络 | 可访问公开行情源 | 稳定宽带 | 数据采集依赖东财、雪球等外部源 |

**必需软件**

- Python 3.12。
- `venv` / `pip`。
- 项目依赖：`quant_system/requirements.txt`。
- Git。
- `pytest`，用于回归测试。

**可选软件**

- MySQL：用于元数据、财报、研报、审计日志等结构化存储。
- Redis：用于盘中实时缓存和任务状态。
- Nginx / 静态文件服务：用于部署报表页面。
- GitHub Pages：用于静态看板发布。

### 6.2 生产 / 长期运行环境

| 场景 | 推荐配置 | 适用范围 |
|------|----------|----------|
| 个人研究 / 每日任务 | 2 核 CPU、4-8 GB RAM、20 GB SSD | 自选股、日线、静态报表 |
| 100-500 股票批量研究 | 4 核 CPU、8-16 GB RAM、50 GB SSD | 多因子、回测、预测、报表 |
| 全市场日线研究 | 8 核 CPU、32 GB RAM、100 GB+ SSD | 全 A 批量因子、滚动回测 |
| 盘中监控 | 4 核 CPU、8 GB RAM、Redis 可选 | 分钟线轮询、实时看板 |
| 实盘辅助 | 独立主机 / VPS、稳定网络、日志备份 | 订单、风控、审计、告警 |

### 6.3 Agent / LLM 运行需求

**接入托管 LLM API**

- 不需要本地 GPU。
- 需要稳定网络和 API Key。
- 需要单独配置密钥，不得写入代码仓库。
- 需要请求预算和调用频率限制。
- 所有 LLM 输出必须落审计日志，不能直接触发实盘交易。

**接入本地 LLM**

- 仅用于离线解释和研究，不建议作为实盘路径首选。
- 7B 级模型建议 16-32 GB 内存或 8 GB+ 显存。
- 14B 及以上模型建议 32-64 GB 内存或更高显存。
- 本地模型输出仍必须经过规则校验和风控过滤。

### 6.4 配置与密钥要求

- 数据源、数据库、Redis、LLM、券商接口密钥必须通过环境变量或本地配置注入。
- `.env`、密钥文件、券商证书不得提交到仓库。
- 实盘交易配置必须与研究配置隔离。
- 生产环境需要保留任务日志、预测日志、Agent 审计日志和订单日志。

---

## 七、Agent 接入方案

### 7.1 当前 Agent 状态

当前系统已具备规则型 Agent：

- `agent/context.py`：读取股票、因子、信号、舆情、增强数据、预测、回测和质量报告。
- `agent/stock_explainer.py`：解释选股依据。
- `agent/strategy_diagnosis.py`：诊断策略表现。
- `agent/predict_review.py`：复核预测方向、证据和风险。
- `agent/orchestrator.py`：编排并输出 Agent 报告。
- `tasks/agent_job.py`：批量生成 Agent 报告。

该 Agent 不调用 LLM，不自动下单，适合作为外部 Agent 接入前的可审计解释层。

### 7.2 外部 Agent 接入目标

- 自动总结个股机会和风险。
- 解释预测结论背后的证据链。
- 诊断策略失效原因。
- 生成每日复盘和任务异常摘要。
- 给出参数优化建议。
- 辅助生成研究报告。

### 7.3 接入架构

```text
量化数据层
  → Agent Context Builder
  → Evidence Package（证据包）
  → LLM / 外部 Agent Provider
  → Policy Guard（权限与合规校验）
  → Agent Report
  → Web 看板 / 审计日志 / 人工确认
```

**建议新增模块**

```text
quant_system/
  agent/
    providers/
      base.py
      llm_api.py
      local_llm.py
    prompts/
      stock_review.md
      strategy_review.md
      daily_brief.md
    policy.py
    schemas.py
    audit.py
```

### 7.4 Agent 输入证据包

外部 Agent 不应直接自由读取全仓库数据，而应消费结构化证据包：

```json
{
  "code": "600378",
  "trade_date": "2026-07-06",
  "stock": {},
  "factors": {},
  "signal": {},
  "prediction": {},
  "backtest": {},
  "quality": {},
  "sentiment": {},
  "enhance": {},
  "risk_limits": {},
  "allowed_actions": ["analyze", "summarize", "suggest"]
}
```

**输入要求**

- 只提供与任务相关的数据。
- 附带数据版本、因子版本、策略版本。
- 明确 `allowed_actions`。
- 缺失数据必须显式标记，禁止让 Agent 猜测。

### 7.5 Agent 输出格式

```json
{
  "summary": "",
  "direction_view": "bullish|bearish|neutral",
  "confidence": "low|medium|high",
  "evidence": [],
  "risks": [],
  "failure_conditions": [],
  "suggested_actions": [],
  "forbidden_actions": [],
  "requires_human_review": true
}
```

**输出要求**

- 必须引用证据包字段。
- 必须说明风险和失效条件。
- 不能输出确定性涨跌结论。
- 不能绕过风控给出下单指令。
- 涉及实盘动作时必须 `requires_human_review = true`。

### 7.6 权限与安全边界

| 权限等级 | 允许能力 | 禁止能力 |
|----------|----------|----------|
| L0 只读分析 | 读取证据包、生成解释 | 修改数据、下单 |
| L1 研究建议 | 生成策略建议、参数建议 | 自动改生产参数 |
| L2 模拟辅助 | 生成模拟交易建议 | 实盘下单 |
| L3 实盘辅助 | 生成待确认订单建议 | 绕过人工确认和风控 |
| L4 自动交易 | 仅在显式授权后可用 | 默认禁用 |

当前阶段只建议开放 L0-L1；模拟交易完成后再考虑 L2。

### 7.7 接入步骤

1. 固化 `EvidencePackage` 和 `AgentReport` JSON Schema。
2. 增加 `agent/providers/base.py` 抽象接口。
3. 接入一个托管 LLM Provider，密钥走环境变量。
4. 增加 `agent/policy.py`，校验输出是否越权。
5. 增加 `agent/audit.py`，保存输入、输出、模型、时间、版本。
6. 在 `agent_job` 中增加 `--provider rule|llm` 参数，默认仍使用 `rule`。
7. 将 LLM Agent 输出接入 `reports/agent/`，并保留规则型 Agent 作为兜底。
8. 增加测试：缺字段、低质量数据、越权输出、无证据结论、LLM 超时降级。

### 7.8 验收标准

- 无 API Key 时系统仍可使用规则型 Agent。
- LLM 调用失败时自动降级为规则型 Agent。
- Agent 输出必须包含证据、风险、限制和免责声明。
- 所有 Agent 输入输出可审计。
- Agent 不得直接修改数据、策略参数或触发交易。

---

## 八、通用约束与风险声明

### 8.1 工程约束

- 所有关键流程必须可重跑、可追溯、可审计。
- 策略、因子、数据版本需要在回测报告和实盘日志中记录。
- 任何数据源失败、降级、补爬、字段异常都必须进入日志。
- 回测和实盘应尽量共享成交约束、风控规则和标的过滤规则。

### 8.2 投资与合规风险

- 系统输出不构成投资建议，仅作为研究和辅助决策工具。
- 回测收益不代表未来收益。
- 数据源异常、延迟或字段变更可能导致信号失真。
- 实盘交易必须由用户主动授权，并保留人工干预和熔断机制。
- 涉及第三方数据采集时，需要遵守对应平台的服务条款和数据使用边界。

---

## 附录 A：近期落地优先级建议

| 优先级 | 任务 | 说明 | 状态 |
|--------|------|------|------|
| P0-1 | 交易日历 + 标准 K 线 + 数据质量巡检 | 含跨源 diff、质量评分、因子门禁 | ✅ |
| P0-2 | 前复权 + 日线数据补齐 | 750 日补录、backfill 写回 stocks | ✅ |
| P1-1 | 技术因子库 MVP | 含版本与质量门禁 | ✅ |
| P1-1.5 | 初级走势信号输出 | bullish / bearish / neutral，标记未回测 | ✅ |
| P1-2 | 舆情采集 | 东财评论 + 雪球热榜 | ✅ |
| P1-3 | 数据增强 | 估值、分红解禁、北向/两融、指数、跨源引用 | ✅ |
| P2-1 | 单策略回测 MVP | T+1 + 成本 + 绩效 + HTML 报告 | ✅ |
| P2-1.5 | 可验证走势预测 MVP | 5d 方向/概率/置信度 + 回测证据 | ✅ |
| P2-2 | 独立盘中看板页 | `reports/live/index.html` 自动刷新 | ✅ |
| P2-3 | 多因子与多策略 | 技术+情绪+基本面+资金合成；ma_cross / multi_factor | ✅ |
| P2-4 | 回测可信度增强 | ST/流动性过滤、容量约束、收益归因、滚动 OOS | ✅ |
| P2-5 | 单股操作决策 | 买入/持有/减仓/卖出/观望、建议仓位、失效条件 | ✅ |
| P3-1 | 模拟交易 | 虚拟账户、模拟订单、持仓、复盘 | ✅ |
| P3-2 | 风控与组合管理 | 组合净值、行业暴露、账户级风控、再平衡 | ⏳ |
| P4-1 | AI Agent 与产品化 | 选股解释、策略诊断、预测复盘、统一看板 | ✅ 规则型 |

### 附录 A.1 实现状态总览（截至 2026-07-06）

**已实现（可 `./run.sh mvp` 一键跑通）**

| 类别 | 能力 | 入口 / 路径 |
|------|------|-------------|
| 数据 | 交易日历、质量巡检、跨源 diff、750 日补录、行情多源降级 | `inspect`, `backfill`, `market`, `stock` |
| 数据 | 舆情、数据增强（估值/公司行为/资金） | `sentiment`, `enhance` |
| 因子 | 技术因子、初级信号、多因子合成 v1.2 | `factor`, `assets/data/factors/` |
| 策略 | MA 金叉、多因子策略 | `backtest --strategy` |
| 回测 | T+1、成本、涨跌停、容量约束、收益归因、滚动验证 | `assets/data/backtest/` |
| 预测 | 5d 可验证预测 + 风险标记 | `predict`, `reports/predict/` |
| 决策 | 单股操作建议、仓位建议、理由、风险、失效条件 | `decision`, `reports/decision/` |
| 模拟交易 | 虚拟账户、决策优先调仓、订单、持仓、交易复盘 | `simtrade`, `reports/trading/` |
| 可视化 | 大盘/个股/因子/增强/回测/预测/盘中/Agent 看板 | `reports/` |
| Agent | 选股解释、策略诊断、预测复盘（规则型，不自动下单） | `agent`, `reports/agent/` |

**部分实现 / 有局限**

| 项 | 说明 |
|----|------|
| 行情采集 | 东财/新浪受网络与代理影响，已支持自选股逐只降级与缓存 |
| 舆情 | 无稳定单股帖子 API，东财评论 + 雪球热榜代理 |
| 数据增强 | 北向/两融等接口可能失败，写入 `limitations` |
| 多因子回测 | 长周期回测仅用 K 线技术分，情绪历史未纳入 |
| Agent | 规则型解释，未接入 LLM；无自动补爬触发 |
| 存储 | MySQL/Redis 预留，默认 JSON 本地存储 |

**未实现（后续可选）**

| 项 | 文档优先级 |
|----|------------|
| 组合管理、行业暴露、账户级风控、再平衡 | P3-2 |
| 实盘券商接口、交易前风控网关 | Next 6 |
| 行业轮动、事件驱动、参数敏感性分析 | Next 3 扩展 |
| 机构/研报/产业链因子、LLM Agent | Phase 1–5 扩展 |
| Tick 级回测、机器学习模型 | MVP 范围外 |

**CLI 速查**

```bash
./run.sh mvp      # 全链路
./run.sh agent    # Agent 分析 + 看板
./run.sh enhance  # 数据增强
./run.sh factor   # 多因子重算
./run.sh decision # 单股操作建议 + 看板
./run.sh simtrade # 模拟交易 + 看板
```

---

## 九、版本迭代记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v2.7 | 2026-07-06 | 实现单股 Decision Engine MVP：汇总预测、因子、回测、Agent、质量和模拟持仓，输出买入/持有/减仓/卖出/观望、建议仓位、理由、风险、失效条件，并接入模拟交易 |
| v2.6 | 2026-07-06 | 实现 P3-1 模拟交易 MVP：虚拟账户、预测驱动调仓、模拟订单、持仓盯市、交易复盘、模拟交易看板与报表入口 |
| v2.5 | 2026-07-06 | 补充当前实现检查、Agent 代码稳健性优化记录、软硬件与运行环境需求、外部 Agent 接入架构、证据包、权限边界和验收标准 |
| v2.4 | 2026-07-05 | 按既有执行计划吸收“优秀量化系统”能力蓝图，补充数据、因子、策略、回测、预测、风控、组合、交易、Agent、Web、监控审计等后续优化方向 |
| v2.3 | 2026-07-01 | 走势预测 L2 MVP：5d 方向/概率/置信度、回测证据链、预测汇总页 |
| v2.2.1 | 2026-07-01 | 回测 MVP：引擎、MA 金叉策略、绩效指标、HTML 报告 |
| v2.2 | 2026-07-01 | 补充阶段能力矩阵、预测能力分级、走势预测闭环、预测输出边界，以及 Phase 2 初级信号和 Phase 3 可验证预测标准 |
| v2.1.1 | 2026-07-01 | 代码审查补缺：质量评分、因子版本、质量门禁、backfill 写回、调度与 CI 对齐 |
| v2.1 | 2026-07-01 | 补充 MVP 边界、数据契约、因子规范、回测成交规则、风控前置、Agent 权限边界、风险声明与近期优先级收敛 |
| v2.0 | 2026-07-01 | 建立增强版需求文档，定义数据、因子、回测、实盘、Agent 和 Web 可视化阶段路线 |
